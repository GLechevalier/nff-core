use anyhow::Result;

pub fn run() -> Result<()> {
    // Autonomous serial-analysis + repair loop — not yet ported to the Rust binary.
    println!("nff connect is not implemented yet — see docs/ROADMAP.md");
    Ok(())
}
