"""nff connect — autonomous repair loop."""

import click


@click.command()
def connect():
    """Autonomous repair loop — monitor the device and attempt repairs on crash."""
    click.echo("nff connect is not implemented yet — see docs/ROADMAP.md")
